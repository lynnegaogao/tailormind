/// <reference types="dom-speech-recognition" />
import { Translations } from '../../types/options';
export type ExtractFunc = (event: SpeechRecognitionEvent, finalTranscript: string, translations?: Translations) => {
    interimTranscript: string;
    finalTranscript: string;
    newText: string;
};
export declare class WebSpeechTranscript {
    static extract(event: SpeechRecognitionEvent, finalTranscript: string, translations?: Translations): {
        interimTranscript: string;
        finalTranscript: string;
        newText: string;
    };
    static extractSafari(event: SpeechRecognitionEvent, _: string, translations?: Translations): {
        interimTranscript: string;
        finalTranscript: string;
        newText: string;
    };
}
